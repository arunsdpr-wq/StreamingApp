apiVersion: v1
kind: Service
metadata:
  name: {{ include "streaming-chart.fullname" . }}
  labels:
    {{- include "streaming-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "streaming-chart.selectorLabels" . | nindent 4 }}
